# MNIST-Digit-Classifier-using-TensorFlow
A simple machine learning project that trains a neural network to recognize handwritten digits (0–9) using the MNIST dataset. This project is built using TensorFlow &amp; Keras, and runs perfectly in Google Colab or VS Code.
